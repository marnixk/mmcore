name: RANDOMIZE
kind: command

Seed the RND generator. With no n, uses the millisecond timer. RND(-n) also sets
the seed.

RANDOMIZE [n]

  n - seed value (optional)

Example:

    RANDOMIZE 1 : PRINT RND
