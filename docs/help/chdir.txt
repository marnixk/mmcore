name: CHDIR
kind: command

Change the current directory. A drive letter such as "C:" or "A:/DATA" also
selects that drive. CWD$ returns the current path.

CHDIR path$

  path$ - directory or drive path

Example:

    CHDIR "A:"
    PRINT CWD$
