name: CALL
kind: command

Call a SUB (or FUNCTION) by name. Arguments are not implemented; use a bare CALL
name. See ~SUB~.

CALL name

  name - SUB or FUNCTION name

Example:

    10 SUB HI
    20 PRINT 42
    30 END SUB
    40 CALL HI
