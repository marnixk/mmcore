name: CALL
kind: command

Call a SUB (or FUNCTION) by name. Arguments are not implemented; use a bare
CALL name. See ~SUB~.

CALL name

  name - SUB or FUNCTION name

Example:

    SUB HI
      PRINT 42
    END SUB
    CALL HI
