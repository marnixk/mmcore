name: CALL
kind: command

Call a SUB (or FUNCTION) by name. Use CALL name with no parameters. See ~SUB~.

CALL name

  name - SUB or FUNCTION name

Example:

    SUB HI
      PRINT 42
    END SUB
    CALL HI
