name: REBOOT
kind: command
alias: RESTART

Restart the Raspberry Pi. Saves settings, stops audio, and unmounts drives
first. Ctrl+Alt+Del on a USB keyboard does the same. Control does not return to
the prompt; the system reboots.

REBOOT
RESTART

Example:

    REBOOT
    RESTART
