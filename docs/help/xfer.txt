name: XFER
kind: command

Receive nbytes of raw serial data and write them to a file. Prints <<XFER>> when
ready to read. Bytes are stored as-is, including NUL.

XFER path$, nbytes

  path$ - destination file
  nbytes - number of bytes to receive

Example:

    XFER "A:/DATA.BIN", 16
