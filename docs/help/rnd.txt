name: RND
kind: command

Return a pseudo-random number in the range 0 <= x < 1.

value = RND
value = RND(seed)

  seed - a negative value reseeds the generator

Use ~RANDOMIZE~ to reseed from the clock. See also ~MATH~.

Example:

    PRINT RND
    PRINT INT(RND * 6) + 1      ' 1..6
