name: DIR
kind: command
alias: LS, LIST FILES

List files in the current directory, or matching an optional path/wildcard.
For the dual-pane file manager, use ~FILES~.

DIR [spec$]
LS
LIST FILES

  spec$ - path or wildcard (optional)

Example:

    DIR
    DIR "A:/*.PNG"
    LS
    LIST FILES
