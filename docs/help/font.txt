name: FONT
kind: command

Select the font number and optional integer scale used by TEXT. The default
HDMI face is 8x16 IBM VGA CP437 (the same glyphs as TERM).

FONT n [, scale]

  n - font number
  scale - integer scale (minimum 1; optional)

Example:

    FONT 1, 2
