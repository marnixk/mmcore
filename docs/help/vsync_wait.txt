name: VSYNC_WAIT
kind: command

Wait until the next display refresh (about 16 ms at 60 Hz). Use at the end of
a game or animation loop to cap the frame rate. Ctrl+C / BREAK aborts a running
program.

VSYNC_WAIT

Example:

    DO
      PAGE COPY 1 TO 0
      VSYNC_WAIT
    LOOP
