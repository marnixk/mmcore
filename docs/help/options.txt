name: OPTIONS
kind: command

Connect to Wi-Fi using the SSID and password stored by OPTION WIFI.

OPTIONS WIFI

?WIFI not configured if no credentials are stored. When connected, prints
Connected to '<SSID>' as <ip>. The password is never printed.

Example:

    OPTION WIFI "MyNet", "secret"
    OPTIONS WIFI
