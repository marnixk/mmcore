# A:/apps

Runnable BASIC programs seeded from `ramdisk/apps/`. Run one with
`RUN "A:/apps/HELLO.BAS"`.

`TDFDEMO.BAS` prints a banner with a TheDraw font from `A:/fonts/tdf/` using
`A:/lib/TDF.BAS`; run it with `RUN "A:/apps/TDFDEMO.BAS"`.
